language: node_js
node_js:
- "13"

dist: trusty

addons:
  sonarcloud:
    organization: "tomdatbenik"

script:
  # other script steps might be done before running the actual analysis
  - sonar-scanner
